{
  "watch1": {
    "scenarios": [
      "features/schemas.feature:A shared schema is valid JSON Schema"
    ]
  },
  "watch2": {
    "scenarios": [
      "features/run-loop.feature:A role dispatch writes an accounting record normalized from the runtime's own response"
    ]
  },
  "watch3": {
    "scenarios": [
      "features/run-loop.feature:An accounting record conforms to its schema"
    ]
  },
  "watch4": {
    "scenarios": [
      "features/run-loop.feature:Runner completes a bounded pass on each supported runtime and reports each gate's verdict"
    ]
  }
}
